# 決策樹（living）

圖由程式產生，**不要手改** `.mmd`。

```text
python -m vai_hold gen-tree
```

- 全圖：`Design/generated/decision_tree.mmd`
- 規則 → 檔案:函式:行號：`Design/generated/locator.md`

查單一 case：從 log 跑 `vai_hold.investigate.render_case(text, lot_id)` 得到業務路徑 + 決策脊柱兩張 mermaid。
